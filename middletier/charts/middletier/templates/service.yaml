apiVersion: v1
kind: Service
metadata:
  name: {{ template "middletier.fullname" . }}
  labels:
    app: {{ template "middletier.name" . }}
    chart: {{ template "middletier.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "middletier.name" . }}
    release: {{ .Release.Name }}
